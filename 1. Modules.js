let Airplane = {}; // creates the object that represents the module Airplane.
// The statement contains an uppercase variable named Airplane which is set equal to an object.
Airplane.myAirplane = 'StarJet'; // is defined as a property of the Airplane module.
// We add data to the Airplane object by setting properties on that object and giving the properties a value.
// StarJet is the value stored in the Airplane.myAirplane property.
module.exports = Airplane; // exports the Airplane object as a module. module is a variable that represents the module, and exports exposes the module as an object.