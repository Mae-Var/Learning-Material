const fruitz = ['mango', 'papaya', 'pen', 'pineapple', 'apple', 'pen'];

fruitz.forEach(function(fruitzItem) {
  console.log(' - ' + fruitzItem);
});

// New code below:

const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below

fruits.forEach(function(singleFruit) {
  console.log('I want to eat a ' + singleFruit);
});
